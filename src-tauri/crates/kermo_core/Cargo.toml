[package]
name = "kermo_core"
version = "2.0.0"
edition = "2021"
license = "Unlicense"
description = "KermoLauncher domain logic (install, launch, catalog)"

[dependencies]
percent-encoding = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
url = "2"
regex = "1"
dirs = "6"

[dev-dependencies]
tempfile = "3"
